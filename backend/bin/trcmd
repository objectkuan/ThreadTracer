#!/bin/bash
source ./trconfig.sh
cmd="./trace-cmd $@"
bash -c "$cmd"

echo "Finished"
echo 0 > $TRACEDIR/tracing_on

# perf's place
perf_pid_file=$OUTDIR/perf.pid
perf_pid=`cat $perf_pid_file`
kill $perf_pid
